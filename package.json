{
  "name": "splitsphere-root",
  "version": "1.0.0",
  "description": "Root proxy package for monorepo deployment",
  "scripts": {
    "build": "npm install --prefix server",
    "start": "node server/server.js"
  }
}
